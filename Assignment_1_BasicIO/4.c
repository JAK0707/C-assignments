#include <stdio.h>

// Program to input a character and display it.
int main() {
    char ch;
    // Input a character
    printf("Enter a character: ");
    scanf(" %c", &ch);
    // Display the input character
    printf("You entered: %c\n", ch);
    return 0;
}
