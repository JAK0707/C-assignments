#include <stdio.h>

// Program to input a decimal number and display it.
int main() {
    float num;
    // Input a decimal number
    printf("Enter a decimal number: ");
    scanf("%f", &num);
    // Display the input decimal number
    printf("You entered: %f\n", num);
    return 0;
}